export default class Talent {
  name: string;
  profile: string;
  image: string;
  color: string;

  constructor(name: string, profile: string, image: string, color: string) {
    this.name = name;
    this.profile = profile;
    this.image = image;
    this.color = color;
  }
}
